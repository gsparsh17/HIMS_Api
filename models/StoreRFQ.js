const mongoose = require('mongoose');
const schema = new mongoose.Schema({ hospitalId: { type: mongoose.Schema.Types.ObjectId, ref: 'Hospital', required: true, index: true }, rfqNumber: { type: String, required: true, index: true }, purchaseRequisitionId: { type: mongoose.Schema.Types.ObjectId, ref: 'StorePurchaseRequisition', required: true }, supplierIds: [{ type: mongoose.Schema.Types.ObjectId, ref: 'Supplier' }], lines: [{ itemId: { type: mongoose.Schema.Types.ObjectId, ref: 'StoreItem', required: true }, description: String, quantity: Number, requiredBy: Date, specifications: String }], issueDate: { type: Date, default: Date.now }, responseDueDate: Date, terms: String, status: { type: String, enum: ['Draft', 'Issued', 'Responses Received', 'Compared', 'Awarded', 'Closed', 'Cancelled'], default: 'Draft', index: true }, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, version: { type: Number, default: 1 } }, { timestamps: true });
schema.index({ hospitalId: 1, rfqNumber: 1 }, { unique: true });
module.exports = mongoose.model('StoreRFQ', schema);
