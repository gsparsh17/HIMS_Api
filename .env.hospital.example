# ==============================================================
# MEDIQLIQ HOSPITAL ON-PREMISE DEPLOYMENT
# Copy to .env, replace every placeholder, and keep it readable
# only by the service account (chmod 600 .env).
# ==============================================================
NODE_ENV=production
APP_ROLE=HOSPITAL
HOST=127.0.0.1
PORT=5000
TRUST_PROXY_HOPS=1

# Same-origin Nginx deployment
FRONTEND_URL=https://hims.hospital.local
CORS_ORIGINS=https://hims.hospital.local
AUTH_COOKIE_NAME=hims_access_token
JSON_BODY_LIMIT=10mb
API_RATE_LIMIT_PER_MINUTE=600

# Local MongoDB server. Percent-encode special characters in the password.
MONGO_URI=mongodb://mediqliqApp:ENCODED_PASSWORD@192.168.10.30:27017/hims?authSource=hims
JWT_SECRET=REPLACE_WITH_AT_LEAST_64_RANDOM_CHARACTERS
DISABLE_PERMISSION_CHECKS=false
ALLOW_PUBLIC_HOSPITAL_REGISTRATION=false
ENABLE_DEMO_LOGIN=false

# Local protected file storage. These paths must be on backed-up storage.
UPLOAD_DIR=/srv/mediqliq/uploads
UPLOAD_TMP_DIR=/srv/mediqliq/tmp

# External services are disabled by default for offline/on-premise operation.
# Enable only after credentials, firewall rules, internet access and hospital
# approval are ready.
ABDM_ENV=sandbox
ABDM_ENABLE_M1=false
ABDM_ENABLE_M2=false
ABDM_ENABLE_M3=false
ABDM_MASTER_URL=
ABDM_HFR_FACILITY_ID=
ABDM_HIP_ID=
ABDM_TENANT_CODE=
ABDM_CONNECTOR_KEY_ID=
ABDM_CONNECTOR_SECRET=

ABDM_LINK_OTP_TEST_MODE=false
ABDM_SMS_PROVIDER_URL=
ABDM_SMS_PROVIDER_BEARER_TOKEN=
ABDM_SMS_TEMPLATE_ID=ABDM_LINK_OTP
ABDM_SMS_TIMEOUT_MS=10000

EMAIL_USER=
EMAIL_PASS=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Authentication abuse protection
LOGIN_RATE_LIMIT_PER_15_MINUTES=20
PASSWORD_RECOVERY_RATE_LIMIT_PER_HOUR=5
